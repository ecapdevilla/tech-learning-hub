11TH GRADE · START FROM ZERO

Adds one additional project card to BOTH:
- 11th Blue
- 11th White

It does NOT replace or edit the existing Blue student projects.

The new card is for:
- teams that have not started,
- students who were absent,
- missing groups,
- teams with no usable evidence,
- teams that need to restart correctly.

The shared Start From Zero guide covers:
problem + ODS -> solution -> architecture -> ESP32 -> Wokwi -> web dashboard -> integration -> testing -> documentation.

Install from tech-learning-hub root:
.\phase-11th-start-from-zero-v1\install-11th-start-from-zero.ps1
